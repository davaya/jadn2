{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://jadn-im.cc/v2.0/schema/jadn-structure",
  "description": "Validates structure of a JADN schema, does not check values",
  "$ref": "#/$defs/Schema",
  "$defs": {
    "Schema": {
      "type": "object",
      "required": [
        "types"
      ],
      "additionalProperties": false,
      "properties": {
        "meta": {
          "$ref": "#/$defs/Metadata"
        },
        "types": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/Type"
          }
        }
      }
    },
    "Metadata": {
      "type": "object",
      "required": [
        "package"
      ],
      "properties": {
        "package": {"$ref": "#/$defs/Uri"},
        "prefixes": {"$ref": "#/$defs/Prefixes"},
        "roots": {"$ref": "#/$defs/Roots"},
        "config": {"$ref": "#/$defs/Config"}
      },
      "unevaluatedProperties": {"type": "string"}
    },
    "Prefixes": {
      "type": "array",
      "items": {"$ref": "#/$defs/Prefix"}
    },
    "Prefix": {
      "type": "array",
      "prefixItems": [
        {"$ref": "#/$defs/Px"},
        {"$ref": "#/$defs/Uri"}
      ]
    },
    "Roots": {
      "type": "array",
      "items": {"$ref": "#/$defs/TypeName"}
    },
    "Config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$MaxBinary": {"type": "integer", "minValue": 1},
        "$MaxString": {"type": "integer", "minValue": 1},
        "$MaxElements": {"type": "integer", "minValue": 1},
        "$Sys": {"type": "string", "minLength": 1, "maxLength": 1},
        "$TypeName": {"type": "string", "minLength": 1, "maxLength": 127},
        "$FieldName": {"type": "string", "minLength": 1, "maxLength": 127},
        "$Prefix": {"type": "string", "minLength": 1, "maxLength": 127}
      }
    },
    "Px": {"type": "string", "minLength": 1},
    "Uri": {"type": "string", "minLength": 1},
    "Type": {
      "type": "array",
      "minItems": 2,
      "maxItems": 5,
      "prefixItems": [
        {"$ref": "#/$defs/TypeName"},
        {"$ref": "#/$defs/CoreType"},
        {"$ref": "#/$defs/Options"},
        {"$ref": "#/$defs/Description"},
        {"$ref": "#/$defs/Fields"}
      ]
    },
    "TypeName": {"type": "string", "minLength": 1},
    "CoreType": {
      "type": "string",
      "enum": ["Binary", "Boolean", "Integer", "Number", "String",
               "Array", "ArrayOf", "Map", "MapOf", "Record",
               "Enumerated", "Choice"]
    },
    "Options": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "Description": {"type": "string"},
    "Fields": {
      "type": "array",
      "items": {
        "anyOf": [
          {"$ref": "#/$defs/Item"},
          {"$ref": "#/$defs/Field"}
        ]
      }
    },
    "Item": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "prefixItems": [
        {"type": "integer"},
        {"$ref": "#/$defs/ItemValue"},
        {"$ref": "#/$defs/Description"}
      ]
    },
    "ItemValue": {"type": "string"},
    "Field": {
      "type": "array",
      "minItems": 3,
      "maxItems": 5,
      "prefixItems": [
        {"type": "integer"},
        {"$ref": "#/$defs/FieldName"},
        {"$ref": "#/$defs/TypeRef"},
        {"$ref": "#/$defs/Options"},
        {"$ref": "#/$defs/Description"}
      ]
    },
    "FieldName": {"type": "string", "minLength": 1},
    "TypeRef": {"type": "string", "minLength": 1}
  }
}
